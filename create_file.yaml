---
- name: Create a file impersonating to other user
  hosts: all
  gather_facts: false
  tasks:
    - name: Display message as non-admin
      ansible.builtin.debug: 
        msg: This task is executed as Administrator

    - name: Display message as admin
      ansible.builtin.debug:
        msg: This tasks is executed as Non-Administrator
      become: true
